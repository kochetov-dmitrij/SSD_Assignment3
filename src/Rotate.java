public class Rotate implements Action {
    private float angle;

    Rotate(float angle) {
        this.angle = angle;
    }

    @Override
    public Image doAction(Image image) {
        return null;
    }
}
